<!-- <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <title>AWRAM AI — Free Unlimited AI Image Generator</title>
  <meta name="description" content="AWRAM AI — free, unlimited, no sign-up AI image generator with 100+ art styles, batch generation, an AI assistant, community gallery and chat.">
  <meta name="theme-color" content="#0a0a0e">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    html, body { height: 100%; background: #0a0a0e; overflow: hidden; }
    #awramFrame {
      position: fixed; inset: 0;
      width: 100%; height: 100%;
      border: 0; display: block; background: #0a0a0e;
      opacity: 0; transition: opacity .45s ease;
    }
    #awramFrame.ready { opacity: 1; }
    #splash {
      position: fixed; inset: 0; z-index: 10;
      display: flex; flex-direction: column; align-items: center; justify-content: center;
      gap: 16px; background: #0a0a0e; color: #eee;
      font-family: system-ui, -apple-system, "Segoe UI", Roboto, sans-serif;
      transition: opacity .4s ease; pointer-events: none;
    }
    #splash.gone { opacity: 0; }
    #splash .logo {
      width: 64px; height: 64px; border-radius: 18px;
      display: flex; align-items: center; justify-content: center;
      background: linear-gradient(135deg, #ff6b9d, #8b5cf6);
      font-size: 32px; font-weight: 800; color: #fff;
      box-shadow: 0 0 30px rgba(139, 92, 246, .45);
    }
    #splash .name { font-weight: 700; letter-spacing: .12em; font-size: 18px; }
    #splash .spinner {
      width: 22px; height: 22px; border-radius: 50%;
      border: 3px solid rgba(255,255,255,.15); border-top-color: #ff8ab0;
      animation: spin .9s linear infinite;
    }
    @keyframes spin { to { transform: rotate(360deg); } }

    /* Open-in-new-tab button (fallback if iframe is blocked) */
    #openBtn {
      position: fixed;
      right: 18px;
      top: 18px;
      z-index: 20;
      background: linear-gradient(135deg, #ff6b9d, #8b5cf6);
      color: #fff;
      border: 0;
      padding: 8px 12px;
      border-radius: 10px;
      font-weight: 700;
      font-family: inherit;
      box-shadow: 0 6px 18px rgba(0,0,0,.45);
      display: none; /* shown after a short wait so the iframe has a chance to load */
      cursor: pointer;
    }
  </style>
</head>
<body>
  <div id="splash">
    <div class="logo">A</div>
    <div class="name">AWRAM AI</div>
    <div class="spinner"></div>
  </div>
  <button id="openBtn" aria-label="Open generator in new tab">Open generator</button>
  <iframe id="awramFrame"
          title="AWRAM AI Image Generator"
          src="https://053f785f01f7d93281d9ddb316852c8a.perchance.org/awram-ai-image-generator-0-pro#embed"
          allow="clipboard-write; fullscreen; encrypted-media; web-share; picture-in-picture"
          allowfullscreen
          referrerpolicy="no-referrer-when-downgrade"></iframe>
  <script>
    (function () {
      var frame = document.getElementById("awramFrame");
      var splash = document.getElementById("splash");
      var openBtn = document.getElementById("openBtn");
      var done = false;
      function hide() {
        if (done) return;
        done = true;
        frame.classList.add("ready");
        splash.classList.add("gone");
        setTimeout(function () { splash.style.display = "none"; }, 500);
      }
      frame.addEventListener("load", hide);
      // safety net — never leave visitors on a blank screen
      setTimeout(hide, 8000);

      // Show the "Open generator" button after a short delay. Some providers block embedding
      // using headers like X-Frame-Options or CSP; this gives visitors a direct way to open the
      // generator in a new tab if embedding is blocked.
      setTimeout(function () { openBtn.style.display = 'block'; }, 3500);

      openBtn.addEventListener('click', function () {
        try {
          window.open(frame.src, '_blank', 'noopener');
        } catch (e) {
          // fallback: navigate the top window
          window.location.href = frame.src;
        }
      });
    })();
  </script>
</body>
</html>


 -->




